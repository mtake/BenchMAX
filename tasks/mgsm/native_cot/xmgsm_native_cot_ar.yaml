# Generated by utils.py
dataset_name: ar
doc_to_target: '{% if answer is not none %}{{answer[20:]}}{% else %}{{answer_number|string}}{% endif %}'
doc_to_text: '{% if answer is not none %}{{question+"\nالإجابة خطوة بخطوة:"}}{% else %}{{"السؤال: "+question+"\nالإجابة خطوة بخطوة:"}}{% endif %}'
filter_list:
- filter:
  - function: regex
    regex_pattern: الإجابة هي (\-?[0-9\.\,]+)
  - function: take_first
  name: strict-match
- filter:
  - function: regex
    group_select: -1
    regex_pattern: (-?[$0-9.,]{2,})|(-?[0-9]+)
  - function: take_first
  name: flexible-extract
generation_kwargs:
  do_sample: false
  max_gen_toks: 1024
  until:
  - </s>
  - <|im_end|>
include: cot_yaml
task: xmgsm_native_cot_ar
